extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.0",
        import: ["@key","@external"])

type Game @key(fields: "id") {
  id: ID!
  name: String!
  marketTypeID: ID!
}

type Query {
  game(id: ID!): Game
}
